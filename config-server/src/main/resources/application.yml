server:
  port: 3344

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/niqikai/config.git
          search-paths:
            - config



eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/